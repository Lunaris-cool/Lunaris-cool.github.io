/**
 * Lunaris Utilities
 * Example functions for Lunaris-Cool Docs
 */

/** Adds two numbers */
export function add(a: number, b: number): number {
  return a + b;
}

/** Subtracts one number from another */
export function subtract(a: number, b: number): number {
  return a - b;
}